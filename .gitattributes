#newline
# _Default_ behaviour_ setting_, in case users don't have core.autocrlf set.
* text=auto

# text eol=lf
# This setting tells git to normalize the files specified on commit, 
# and always convert them to LF on checkout. You should use this for files 
# that must retain LF endings, even on Windows.

*.py text eol=lf
*.json text eol=lf
*.sh text eol=lf
*.md text eol=lf
*.txt text eol=lf
*.xml text eol=lf
*.prefs text eol=lf
*.example text eol=lf
*.save text eol=lf
*.token text eol=lf

# Declare files that will always have CRLF line endings on checkout.
#*.sln text eol=crlf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
