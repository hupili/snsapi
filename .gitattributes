# Default behaviour setting, in case users don't have core.autocrlf set.
* text=auto

# Explicitly declare text files we want to always be normalized and converted 
# to native line endings on checkout.
# setting to "text" tells git to always normalize the files specified. 
# When committed they are stored with LF, on checkout they are converted 
# to the OS's native line endings.

*.py text eol=lf
*.json text eol=lf
*.sh text eol=lf
*.md text eol=lf
*.txt text eol=lf
*.xml text eol=lf
*.prefs text eol=lf
*.example text eol=lf
*.save text eol=lf
*.token text eol=lf

# Declare files that will always have CRLF line endings on checkout.
#*.sln text eol=crlf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary